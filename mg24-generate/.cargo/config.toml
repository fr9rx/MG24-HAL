# mg24-generate is a host tool (requires std), not embedded code
# Explicitly override workspace's embedded target configuration

[build]
target = "x86_64-pc-windows-msvc"

[alias]
rb = "build --release"
rr = "run --release"
